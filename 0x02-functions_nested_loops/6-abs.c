#include "main.h"


/**
 * _abs  - Prints the absolute value of a integer
 * @num: argument
 * Return: the absolute value of an integer
 */
int _abs(int num)
{
	if (num >= 0)
		return (num);
	else
		return (-num);
}
